"""平台核心的验收测试，一块主题一个模块。

`core` 是三样不相干的东西的集合，测试因此各归各的模块：

* ``test_registry_and_audit`` —— 操作入口注册表（按权限过滤）与审计日志（只读）；
* ``test_interface_translation`` —— 界面英文翻译的兜底检查，连同扫源码与 `.po`
  的那几个工具函数（它们是这一块专有的，不给别处用）；
* ``test_upload_validation`` —— 通用上传校验：解压炸弹、用户可控文本的长度上限。

拆的依据是「读的人为什么打开这个文件」：改注册表的人不该先翻过一百行 `.po`
解析代码才能找到入口测试。
"""
