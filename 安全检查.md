# 安全检查

| | |
|---|---|
| 审查对象 | 竞赛社团管理平台（Django 5.2 单体 + PostgreSQL + gunicorn + Nginx） |
| 代码基线 | `98564c5`（2026-09-24），Python 3.13.11，Django 5.2.17 |
| 审查方式 | 全量源码走查 + 关键路径动态验证（上传校验、模板渲染、`manage.py check --deploy`） |
| 审查范围 | 应用代码、模板、配置、部署工件（systemd / Nginx / shell 脚本） |
| 结论 | **未发现 SQL 注入、命令注入、反序列化、模板注入等可直接控制服务端的漏洞；发现 1 处可稳定复现的远程拒绝服务（图片解压炸弹），以及若干「依赖部署配置才成立」的传输层与可用性缺口** |

---

## 一、先列威胁：这个系统需要防什么

按「资产 → 攻击面 → 威胁」梳理。本平台的资产不是通用互联网业务数据，而是一批**身份数据**与**评审机密**，这决定了威胁排序与普通网站不同。

### 1.1 资产清单

| 资产 | 在哪 | 泄露/被篡改的后果 |
|---|---|---|
| 账号与口令散列 | `accounts_user` | 撞库、横向登录 |
| 实名身份：姓名、学号、学院、专业、手机号、其他联系方式、个人简介 | `accounts_profile` | 学号+手机号是精准诈骗与社工的原料；面向全校成员的通讯录本身就是敏感数据 |
| 项目书、竞赛报名名单、指导老师 | `projects_*`、`competitions_*` | 未发表成果被抢先；组员隐私 |
| **评审意见与评审判定** | `reviews_reviewtask`、`reviews_archivedproposal` | 平台的核心机密：一旦评审人身份与意见对上号，评审制度即失效，且会引发人际冲突；实施细节直指「谁能审谁、怎么绕过」 |
| 内部通知、设备借用记录 | `notices_*`、`equipment_*` | 内部管理信息 |
| 审计日志 | `core_auditlog` | 篡改后追责链条断裂 |
| 数据库与媒体目录备份 | `backups/` | 上述全部的离线副本 |
| 部署机密：`env.sh`（DB 口令、`SECRET_KEY`、超管口令） | 应用目录 | 直接接管系统 |

**贯穿性的业务约束**：[架构文档的 reviews 一篇](docs/architecture/reviews.md) 规定评审身份一律匿名。这不是 UI 偏好，而是一项安全需求——因此「评审人身份会不会从接口、文件名、页面文案、可枚举 ID 里漏出去」属于安全审查范围，本文按此处理。

### 1.2 攻击面

- **公开、无认证**：首页、`/about/`、`/awards/`、`/showcase/`、`/pages/<slug>/`、公开通知列表与详情、`/login/`、`/admin/`
- **认证成员**（管理员发放账号、无自助注册）：`/member/*` 全部、社团空间、设备借用、项目组浏览/申请、竞赛报名、`/media/` 下的全部上传文件
- **项目组联系人**：本项目组的管理、项目书上传、送审
- **评审人 / 初审人 / 超级评审**：评审队列、结论提交、批注版下载、请假
- **管理员**：`/admin/` 全站后台 + 用户/资格管理 + 改派 + 审计只读

### 1.3 威胁清单（按「值得投入多少」排序）

| # | 威胁 | 本系统的具体形态 | 现状 |
|---|---|---|---|
| T1 | **越权访问（IDOR / 水平越权）** | 评审意见、项目书、批注版、内部通知、他人个人资料都靠对象级判定保护 | 见 §3.2，**总体良好** |
| T2 | **匿名性破坏** | 评审人身份经接口、文件名、可枚举 ID、时间线、审计详情泄露 | 见 §4.6，机制大体成立，存在旁路 |
| T3 | **拒绝服务（应用层）** | 单一 gunicorn 进程池、无速率限制、无 WAF；一个可稳定触发的未处理异常即可反复打满工作进程 | 见 §4.1，**已复现** |
| T4 | **中间人 / 会话劫持** | 站点默认以纯 HTTP 交付，登录口令、会话 Cookie、CSRF 令牌全部明文过网 | 见 §4.2，**默认不安全，取决于启用 HTTPS** |
| T5 | **XSS（存储型为主）** | 帖子正文经 Markdown 渲染、通知正文同样；其它字段靠自动转义 | 见 §3.4，以 bleach 收口，无 `|safe` 泄漏 |
| T6 | **CSRF** | 全部写操作 | 见 §3.3，**覆盖完整** |
| T7 | **SQL 注入** | 全站 ORM | 见 §3.1，**未发现** |
| T8 | **暴力破解与撞库** | `/login/`、`/admin/login/` 暴露在公网，无任何锁定/限速 | 见 §4.4 |
| T9 | **上传文件攻击** | 5 条上传通道：头像、图册、帖子图、媒体库、项目书/批注版 | 见 §4.1、§4.5，签名校验齐全，一处异常未捕获 |
| T10 | **权限提升（垂直越权）** | 评审资格、管理员位、后台字段可写范围 | 见 §3.2；**2026-09-26 复查发现后台批量动作两处缺门禁（资格授予=提权路径），已修；设备归还前台那侧仍偏宽，留待定夺** |
| T11 | **信息泄露（公开面）** | 未认证接口暴露内部信息 | 见 §4.7，**未发现** |
| T12 | **供应链与依赖** | Django / bleach / Pillow / psycopg / gunicorn，无锁文件、无 CI | 见 §4.9 |
| T13 | **配置与密钥管理** | `SECRET_KEY`、DB 口令、`env.sh` 权限 | 见 §4.3、§4.8 |
| T14 | **数据完整性与可恢复性** | 备份易被同机勒索/误删、DB 与媒体非原子 | 见 §4.10 |
| T15 | **暴力破解产物：公开面爬取** | 学号/手机号无节制暴露给全体成员 | 见 §4.6 |
| T16 | 点击劫持 / MIME 嗅探 / 目录穿越 / 缓存投毒 / 开放重定向 | 一般性 Web 威胁 | 见 §3.5，**均已覆盖** |

> 说明：T3/T4/T8 里「慢速攻击、连接耗尽、TLS 降级」这类属于 Nginx 与内核层面的问题，本报告只给出「应用侧能做什么」，不展开内核调优。

---

## 二、结论摘要

### 2.1 做得好的地方（不是客套，是后续改动不要破坏的约束）

1. **注入类风险从根上被消掉**：全仓库没有一处 `raw()` / `extra()` / `RawSQL` / `cursor()` / 字符串拼 SQL；唯一的 `mark_safe` 前面有 bleach 白名单（`content/templatetags/rendering.py:67`）。也没有 `eval` / `exec` / `subprocess` / `os.system` / `pickle`。
2. **没有 `csrf_exempt`**，16 个含 POST 表单的模板全部带 `{% csrf_token %}`，写操作视图普遍 `@require_POST`——CSRF 与「GET 触发写」两条一起被堵住。
3. **对象级权限逐视图显式判定**，且把「谁算管理员」「谁能看这个组」收敛到 `core/permissions.py`、`projects/permissions.py` 单点，没有两套口径分叉的隐患。
4. **文件下载不经过 `/media/`**：帖子图片、项目书、批注版、归档版全部由视图做权限判定后再 `FileResponse`（`reviews/views.py:275` 有明确注释说明「Nginx 直出会绕过权限」）。这是本系统最容易踩的坑，作者踩对了。
5. **匿名性有四道具体机制**：批注版与归档版落盘名用 uuid（`reviews/models.py:67`）、下载名中性化（`views.py:85`）、页面只写「评审人 N」（`templates/projects/group_detail.html:154`）、公开内容不进 `sitemap` 也无枚举接口。
6. **并发正确性做得比一般项目好**：设备库存 `select_for_update` + DB `CheckConstraint`；图册配额锁用户行防双标签页超限（`accounts/services.py:127`）；送审锁项目组防同轮冲突（`reviews/services.py:92`）；「一人一轮一席」落在数据库唯一约束（`unique_submission_task_reviewer`）。
7. **审计留痕覆盖业务命令**，且刻意不记敏感明细（`core/audit.py:27` 有注释约定），`ip_address` 不信任 `X-Forwarded-For`。
8. **后台留痕类模型只读**（`core/admin.py:9` `ReadOnlyAdminMixin`）。

### 2.2 风险清单

| 编号 | 风险 | 等级 | 触发前提 |
|---|---|---|---|
| **V1** | 图片解压炸弹触发未捕获异常，单请求即可占用工作进程，无速率限制时可稳定耗尽服务 | **高** | 任意已登录成员（头像/图册/帖子图三条通道） |
| **V2** | 站点默认纯 HTTP 交付：无 HSTS、无 `SECURE_SSL_REDIRECT`、Cookie 非 Secure | **高** | 部署时未启用 HTTPS（模板默认即如此） |
| **V3** | 登录接口无速率限制、无失败锁定；`must_change_password` 账号在改密前可用无限次会话 | **中高** | 暴露公网 |
| **V4** | 公网静态扫描文件（`/admin/` 指纹、`.github/CODEOWNERS`） | 中 | 默认存在 |
| **V5** | 上传原文件名落盘，评审匿名可能经运维/备份/媒体库泄露 | 中 | 批注版替换重建、媒体库附件 |
| **V6** | 6 处用户可控文本无长度上限（评审意见、提交说明、申请理由、组介绍等） | 中 | 任意成员 |
| **V7** | 备份目录与数据库同机且随仓库目录一起 `ReadWritePaths` 放开；DB 与媒体备份非同一快照 | 中 | 主机被入侵 / 恢复演练 |
| **V8** | 依赖无版本锁定、无 CI 自动检查 | 中低 | 长期 |
| **V9** | 评审意见在项目组详情页对全组员可见，且可编辑帖子可回溯改写正文 | 中低 | 制度/流程设计 |
| **V10** | `SECRET_KEY` 落在 `env.sh` 明文且文件权限未由脚本收紧 | 低中 | 主机上有其它低权账号 |
| **V11** | 竞赛报名放弃未校验截止时间，截止后仍可删除报名记录 | 低 | 项目组联系人 |

### 2.3 修复状态（2026-09-25 更新）

本报告的全部发现已在 `security/hardening` 分支上逐条处理，一个阶段一个提交：

| 编号 | 处置 | 落在哪 |
|---|---|---|
| V1 | 已修：接住 `DecompressionBombError`、自定像素上限、校验读副本（Pillow 会关掉调用方的流） | 阶段 1 |
| V2 | 已修：Cookie Secure 默认开；SSL 跳转与 HSTS 做成开关（默认关，理由见 `docs/deploy.md`）；`deploy.sh` 加上线门禁 | 阶段 2 |
| V3 | 已修：`django-axes`，账号与 IP 两维度各算、10 次锁 30 分钟，锁定期正确口令也拒绝 | 阶段 3 |
| V4 | 已修：Nginx 拒绝 `.` 开头的路径 | 阶段 1 |
| V5 | 已修：受保护上传件移到 `protected_media/`（不在 `/media/` 之下），全部经视图取件，落盘名统一 uuid；含数据迁移 | 阶段 4 |
| V6 | 已修：九处可填文本补 `max_length`（表单 + 模型两侧），显式设置上传体积上限 | 阶段 5 |
| V7 | 已修：备份移出应用目录、加 gpg 加密钩子、说明一致性窗口；每季度恢复演练写进文档 | 阶段 6 |
| V8 | 已修：pip-tools 锁定（带哈希，`--require-hashes` 安装）+ CI 三道门 | 阶段 6 |
| V9 | **按你的决定保持现状**：组员可见评审意见、帖子随时可编辑且不留历史，作为已知取舍记录 | 不改代码 |
| V10 | 已修：`env.sh` 权限收紧 600；生产沿用默认 `SECRET_KEY` 时拒绝启动 | 阶段 1/2 |
| V11 | 已修：报名放弃补截止时间校验，与报名、修改同一判据 | 阶段 5 |

V8 原文提到的 `admin.E408` 一句**有误**，实测 `manage.py check` 无任何问题，已在 §4 更正。

### 2.4 生产实地核对（2026-09-25）

修复完成后登进生产机（实例 `i-m5e67qhgou0ks5c8wuy6`，用户 `dmxbvigl`，应用在
`/home/dmxbvigl/applications/702platform`）逐项核对，跑的是 `98564c5`（本分支的基线）。
三处结论与报告写的不同或需要补充：

| 项 | 报告/修复时的假设 | 实地情况 |
|---|---|---|
| V4 `.git` 暴露 | 担心 `.git/` 可被下载 | **实测 `/.git/config` 返回 404**——Nginx 的 `location /` 是反代给 Django，Django 不认这个路径。加的 `deny` 规则是纵深防御，不是补漏 |
| V5 数据迁移 | 「要按停机窗口做，会真实移动文件」 | **生产库里六类受保护文件记录数全为 0**（头像/图册/项目书/帖子图/批注版/归档版），`mediafiles/` 下只有 2 张公开队刊图。迁移是空操作，**不需要停机** |
| V2 Cookie Secure | 以为改代码默认值即可生效 | **生产的 `env.sh` 显式写着 `SESSION_COOKIE_SECURE=0`、`CSRF_COOKIE_SECURE=0`、`PROXY_SSL_HEADER=0`，会覆盖代码默认值**。必须改 `env.sh` 才生效 |

另有两处修复引入的、只有在真实服务器上才暴露的问题，已一并修掉：

1. **备份目录默认值不可用**：我原本把默认值从 `backups/` 改到 `/var/backups/club702`，
   但生产上 `/var` 属 root、`sudo` 需要密码，部署用户建不了那个目录——`deploy.sh` 的
   第一步（备份）就会失败。已把默认值改回 `$APP_DIR/backups`，并在 `backup.sh` 里加了
   「目录不可写就明确报错」；想外置的话在 `env.sh` 里设 `DJANGO_BACKUP_DIR` 并手工建目录。
2. ~~锁文件的哈希不覆盖生产平台~~：**这条是我的误判，已核实锁文件没有问题**。
   `pip-compile --generate-hashes` 会把所有平台的轮子哈希都收进锁文件（`pillow` 106 个、
   `psycopg-binary` 66 个），本地 3.13 生成的锁文件在生产 3.12 上照样能装。实测
   `pip download --require-hashes --python-version 312 --platform manylinux_2_17_x86_64`
   解析成功，11 个包全部按哈希下载——其中 `pillow`/`psycopg_binary` 用的正是 cp312
   manylinux 轮子。我起初用 `grep -A 40` 只看到哈希块前 40 行就下了结论，是看漏了。

`SESSION_COOKIE_SECURE` 与 `DJANGO_PROXY_SSL_HEADER` 的关系也在实地核对后更正：前者
**直接**决定 Cookie 上带不带 `Secure`（Django 内部就是
`secure=settings.SESSION_COOKIE_SECURE`），与代理头无关；代理头影响的是
`request.is_secure()` 与 CSRF 的 Origin 校验。生产上 `PROXY_SSL_HEADER=0` 而 Nginx 明明
发了 `X-Forwarded-Proto`，是个隐患（一旦开 `SECURE_SSL_REDIRECT` 会变成无限重定向），
文档已建议一并打开。

---

## 三、逐类攻击的检查结果

### 3.1 SQL 注入 —— 未发现

**检查方法**：全仓搜索 `raw(` / `extra(` / `RawSQL` / `cursor()` / `.execute(` / 字符串拼 SQL。

**结果**：零命中（唯二两处命中是函数名 `competition_registration_withdraw`、`_draw`，与 SQL 无关）。

**判定**：全站走 ORM 参数化；`discussion/selectors.py:35` 的 `profile__full_name__icontains`、`discussion/forms.py:30` 的 `Lower("name")` 对比都是 ORM 表达式，用户输入不会进入 SQL 文本。**此项无需改动**。

> 唯一需要长期守住的是：今后若引入原生 SQL（例如统计报表、全文检索），必须参数化。当前没有 `django.contrib.postgres` 的 `SearchVector` 用法，也没有 raw 聚合。

### 3.2 越权访问（IDOR / 垂直越权）—— 未发现可利用缺陷

逐条核对对象级判定：

| 入口 | 判定位置 | 结论 |
|---|---|---|
| 项目组详情 / 项目书下载 | `projects/views.py:154,242` → `can_view_group`（staff ∪ 组员 ∪ 评审主张） | 正确 |
| 批注版 / 归档版下载 | `reviews/views.py:263,291` → `can_view_group` | 正确 |
| 评审任务提交 | `reviews/views.py:178` 额外比对 `task.reviewer_id != request.user.pk` | 正确（即使有资格也不能代交别人的任务） |
| 超级评审一票敲定 | `selectors.override_blocker`（提交人/组员/本轮已持任务者均不可） | 正确 |
| 图册/头像改删 | `accounts/views.py:287` 以 `profile=self` 收窄查询集，他人图片一律 404 | 正确 |
| 设备归还 | `equipment/views.py:129` 非管理员强制加 `borrower=request.user` 过滤 | 正确 |
| 内部通知 | `notices/visibility.py` 单一可见性查询集，列表与详情共用 | 正确 |
| 竞赛报名改/放弃 | `competitions/views.py:180,237` → `can_manage_group` | 正确 |
| 建组申请审批 | `projects/views.py:326` → `can_decide_group_create_requests`（仅管理员） | 正确 |
| 板块创建/删除 | `discussion/views.py:328,358` 要求 `is_superuser` | 正确 |

**值得记录的一处设计选择**：`projects/permissions.py:117` `groups_visible_to()` 对「不属于任何项目组、也不是联系人的成员」返回**全部项目组**，以便他们申请加入。因此项目组列表（名称、联系人、简介、学院、指导老师、评审进度）对全体成员可见。这是显式取舍，不是缺陷，但应在制度上确认「未发表项目对全体社员可见」是可以接受的。详情页另有 `can_view_group` 把关，方向正确。

**评级**：无待修项。

#### 补记（2026-09-26）：后台批量动作的门禁

上面那张表逐条核对的是**视图入口**，不包含后台列表页的批量动作——那是另一套准入。
`@admin.action` 不声明 `permissions` 时 Django 对所有人放行
（`_filter_actions_by_permissions` 只过滤带 `allowed_permissions` 的动作），而动作体里
调的 `set_qualification` / `return_borrow` 都是「给程序调用的服务函数」，不看请求是谁
发的，所以这个参数是那个动作**唯一**的 HTTP 门槛。复查确认了两处，**两处的性质不同**：

| 位置 | 症状（实测） | 处置 |
|---|---|---|
| `accounts/admin.py` 六个资格动作 | 不声明 `permissions`，默认对所有人放行。只挂 `view_user` 的只读观察者能在用户列表页拿到动作并提交，给自己或他人发评审／初审／**超级评审**资格——一条提权路径 | `permissions=["change"]`。这一屏的 `has_change_permission` 是 Django 默认实现，查 `accounts.change_user`，正是想要的粒度 |
| `equipment/admin.py` `mark_returned` | 同样默认放行；**而且**这一屏的 `has_change_permission` 被覆写成 `is_admin`，即 `is_staff`（`core/permissions.py:21`），所以 `permissions=["change"]` 在这里等于没声明、任何 staff 都过——只挂 `view_equipmentborrow` 的观察者提交后借用记录真的变成已归还、库存真的加回去 | `permissions=["change_equipmentborrow"]` + `has_change_equipmentborrow_permission()`（Django 的 `admin.E129` 系统检查**强制**要求这个配套方法），查真实的模型权限 |

**由此订正本报告原先「未发现可利用缺陷」的结论**：
**任何 `is_staff` 账号（包括只挂 view 权限的只读观察者）可以归还任何人的设备借用记录**
——后台批量动作与前台 `/equipment/borrows/<id>/return/` 两条路都是。前台的
`equipment/views.py:132` 只按 `is_admin` 放行（非管理员才收窄成 `borrower=request.user`），
而后台那屏的 `has_change_permission` 又是同一个 `is_admin`。两处**同源**，所以这不是
两处笔误，而是「谁算管理员」这一个定义在设备归还上的口径偏宽：`is_admin` 等于
`is_staff`，而 staff 里可以有只读观察者。

**本轮只做了一侧窄化，这个取舍要说明白**：修的是**后台**动作（那里改的是别人的记录与
公共库存，且这一屏给了一次操作一批人的杠杆）。**前台仍旧按 `is_admin` 放行**——那样
staff 用手机上的成员界面还自己借的设备仍然顺畅；若改成模型权限，一个只管后台的观察者
会发现自己连自己的借用都还不掉。

因此**设备归还的完整收口留待定夺**：要不要把前台也一并收紧（例如「管理员 OR 本人」
两条并集，让观察者只能还自己的）？我在本轮没有替你决定，已按现状提交并在 PR 里标注。
在此之前，「staff 可归还他人设备」作为一个**已知的、有意的**现状存在。

回归用例见 `accounts.tests.RoleRosterAcceptanceTests.test_view_only_observer_cannot_submit_a_qualification_action`
与 `equipment.tests.EquipmentAcceptanceTests.test_view_only_observer_cannot_mark_a_borrow_returned`
（两条都验过：撤掉修复即失败）。

> 排查方法记一笔：全仓 `@admin.action` 只有这两处（`grep -rn "@admin.action"`），已逐处确认。
> 另外 `equipment/admin.py` 原先有一条注释把「非管理员进不了列表页」当作这道防线，
> 而 `is_admin` 等于 `is_staff`、staff 里可以有只读观察者——前提不成立，注释与结论都已改正。

### 3.3 CSRF —— 覆盖完整

- 中间件在位（`config/settings.py:61`），无任何 `@csrf_exempt`。
- 模板核对：16 个含 `method="post"` 的模板，POST 表单数 = `{% csrf_token %}` 数（`space.html` 7/7、`profile.html` 7/7、`group_manage.html` 8/8 等）。
- 改密视图的 `next` 参数用 `url_has_allowed_host_and_scheme` 校验（`accounts/views.py:97`），开放重定向一并堵住。

**评级**：无待修项。

### 3.4 XSS —— 存储型风险已收口

**检查方法**：搜索 `|safe`、`mark_safe`、`autoescape off`、内联事件处理器。

- 全仓只有一个 `mark_safe`，在 `content/templatetags/rendering.py:67`，其输入经过 `bleach.clean(tags=…, attributes=…, protocols={"http","https","mailto"}, strip=True)`。
- 6 处使用点（通知正文、内容页正文、获奖人、成员风采简介、帖子正文）全部走同一过滤器。
- 动态实测：

```
输入:  ```html\n<script>alert(1)</script>\n```  +  <a href="javascript:alert(1)">x</a>
markdown 输出: <pre><code class="language-html">&lt;script&gt;…</code></pre> <p><a href="javascript:alert(1)">x</a></p>
bleach 之后 : <code class="language-html">&lt;script&gt;…</code></pre> <p><a>x</a></p>
```

`script` 被转义而非执行，`javascript:` 协议被剥离，`on*` 事件属性不在白名单内。**结论：存储型 XSS 无法从 Markdown 通道构造**。

**两处低危观察（不构成漏洞，建议顺手加固）**：

1. `code` 标签的 `class` 属性白名单过宽，bleach 只校验属性名不校验取值，用户可以指定任意 class（实测 `<code class="anything-I-want x">` 通过）。在本站样式表内没有可被滥用的类名，风险停留在「样式干扰」。建议改为 `callable` 白名单只放行 `language-*`。
2. `ALLOWED_ATTRIBUTES` 允许 `a[href]` 且未强制 `rel`，配合 `_blank` 时理论上存在 `window.opener` 反向控制。当前 Markdown 链接不会带 `target`，实际不可达；若将来加 `target="_blank"` 需同时补 `rel="noopener noreferrer"`。

**评级**：无待修项；上述两条建议见 §5.3。

### 3.5 其余常规攻击 —— 均已覆盖

| 攻击 | 现状 | 证据 |
|---|---|---|
| 点击劫持 | `X_FRAME_OPTIONS = "DENY"` + `XFrameOptionsMiddleware` | `settings.py:66,168` |
| MIME 嗅探 | `SECURE_CONTENT_TYPE_NOSNIFF = True` | `settings.py:167` |
| 目录穿越 / 静态直读 | 模板只用 `{% static %}` 与 `{% url %}`；模板标签 `|basename`、`|file_ext` 只取文件名后缀 | `templates/base.html:8`、`group_detail.html:194` |
| 开放重定向 | `url_has_allowed_host_and_scheme` | `accounts/views.py:97` |
| 命令注入 / 反序列化 / SSTI | 无 `eval`/`exec`/`subprocess`/`pickle`/`yaml.load`；模板由 Django 编译，无用户可控模板 | 全仓搜索零命中 |
| 缓存投毒 | 未使用 `CacheMiddleware` / `vary_on_cookie`，无共享缓存层 | `settings.py:54-67` |
| 会话固定 | 使用 Django `LoginView`，登录时自动 `cycle_key()` | Django 内建 |
| 口令存储 | Django PBKDF2 默认哈希；DB 侧 `scram-sha-256`（`install.sh:179-199`） | 已核对 |

### 3.6 业务逻辑与竞态 —— 表现良好

- 「一人一轮一席」写进数据库唯一约束，绕过服务层也做不到一人两票。
- 「每轮至多一条初审」用条件唯一约束表达。
- 库存扣减、图册配额、送审开轮三处都用了行锁 + 事务内复检，并对 `IntegrityError` 有明确落点。
- `submit_verdict()` 拒绝非 `pending` 任务，被超级评审释放的任务无法再被改写。

**这一层没有发现可用的业务逻辑漏洞。**

---

## 四、必须处理的问题

### V1（高）图片解压炸弹触发未捕获异常 —— 已动态复现

**位置**：`media/validators.py:25`（媒体库图片）、`accounts/validators.py:31,40`（头像、图册）、`discussion/validators.py:48`（帖子图）；根因在 `core/uploads.py:70-77`。

**机制**：`core/uploads.py` 的 `except (UnidentifiedImageError, OSError, SyntaxError)` 没有涵盖 Pillow 的 `DecompressionBombError`。该异常只在 `Image.open()` 读到头部尺寸时抛出（`PIL/Image.py:1324`），且其基类是 `Exception` 而非 `OSError`（已核对 `PIL/Image.py:74`）。

**复现记录**（本机实测，Django 5.2.17 / Pillow 11.3.0）：

```
构造：99 字节 PNG，IHDR 声明 20000×20000（4 亿像素）> 2×MAX_IMAGE_PIXELS
- accounts/forms.AvatarForm（forms.ImageField）      → is_valid()=False，报「不是有效图片」（Django 自己 try 了 Exception，安全）
- discussion/forms.PostForm（MultipleImageField=forms.FileField，自定义校验器）→ 抛出未捕获的 PIL.Image.DecompressionBombError
- discussion/services._validated_images（服务层兜底）  → 同样抛出未捕获异常
```

**影响**：
1. 帖子图与图册这两条通道，上传一个 99 字节的文件即可让视图抛异常。视图未捕获 → Django 记 500 后重新抛出 → 请求以 500 结束。**不是直接的信息泄露**。
2. 真实危害在可用性：异常发生在渲染 500 页面之前，且每次请求都要完整走一遍 multipart 解析。单机 3 个 gunicorn 同步 worker、无速率限制、无 WAF（`deploy/club702.service:13`），持续发送该请求即可反复占满全部 worker，是成本极低的拒绝服务。
3. 媒体库与项目书通道触发条件不同（见 §4.5），但**同一类异常在媒体库路径同样未捕获**。

**修复（`core/uploads.py`，一处收口，四条通道同时生效）**：

```python
from PIL import Image, UnidentifiedImageError
try:
    from PIL.Image import DecompressionBombError
except ImportError:  # 老版本 Pillow
    DecompressionBombError = ()

...
    except (UnidentifiedImageError, OSError, SyntaxError, DecompressionBombError) as exc:
        raise ValidationError(_("上传文件不是有效的图片。")) from exc
```

**配套（纵深防御，建议一并做）**：
- 在 `core/uploads.py` 顶部显式 `Image.MAX_IMAGE_PIXELS = 64_000_000`（约 8000×8000），把阈值写到代码里而不是交给 Pillow 默认值；当前默认约 8948 万像素。
- 若要在校验阶段就拒绝而非等到解码，可在 `Image.open()` 后先读 `image.size` 判上限，再 `verify()`。
- **真正的兜底是限流**（见 V3）：应用层永远会有未预料的异常路径，速率限制才是把「单请求异常」限制成「单请求」的那道闸。

---

### V2（高）站点默认以纯 HTTP 交付，中间人攻击在默认部署下成立

**位置**：`deploy/env.template:33-38`、`deploy/nginx-club702.conf`、`config/settings.py:164-174`。

**现状**：
- `env.template` 默认 `DJANGO_SESSION_COOKIE_SECURE=0`、`DJANGO_CSRF_COOKIE_SECURE=0`、`DJANGO_PROXY_SSL_HEADER=0`，`SSL_CERT_PATH`/`SSL_KEY_PATH` 留空。
- `install.sh:321-323` 在证书留空时**删除 443 整块配置**，只保留 80。也就是说：按模板默认值走完 `install.sh`，站点就是纯 HTTP。
- `SECURE_HSTS_SECONDS`、`SECURE_SSL_REDIRECT` 在 `settings.py` 中根本没有出现（默认 `0`/`False`）。

**`manage.py check --deploy` 实测输出**（`DJANGO_DEBUG=0` 时 6 项，去掉 DEBUG 提示仍有 5 项）：

```
security.W004  SECURE_HSTS_SECONDS 未设置
security.W008  SECURE_SSL_REDIRECT 未设为 True
security.W009  SECRET_KEY 使用 django-insecure- 前缀的默认值
security.W012  SESSION_COOKIE_SECURE 未设为 True
security.W016  CSRF_COOKIE_SECURE 未设为 True
```

**影响**：HTTP 明文通道上，登录口令、会话 Cookie、CSRF 令牌可被同网段（校园网、公共 Wi-Fi、运营商链路）直接读取或篡改，会话可被劫持；`must_change_password` 的首次改密流程同样在明文里走完。对一个承载实名身份与评审机密的系统，这是当前**最现实的**攻击面。

**修复**：
1. 部署时启用 HTTPS（`docs/deploy.md:97` 已有步骤），`env.sh` 中同时打开：
   ```
   DJANGO_SESSION_COOKIE_SECURE=1
   DJANGO_CSRF_COOKIE_SECURE=1
   DJANGO_PROXY_SSL_HEADER=1
   DJANGO_CSRF_TRUSTED_ORIGINS=https://<域名>
   ```
2. Nginx 80 端口从「与 443 并存」改为**整站 301 跳转 443**（当前 80 是完整服务块）。
3. 在 `settings.py` 按环境变量补上 `SECURE_SSL_REDIRECT` 与 `SECURE_HSTS_SECONDS`（初值建议 60 秒，确认全站可用后再升到 31536000），使 `check --deploy` 能作为部署门禁。
4. 建议把 `python manage.py check --deploy` 加进 `deploy.sh` 的上线步骤，配置退化时直接中止。

---

### V3（中高）认证接口无速率限制、无失败锁定

**现状**：全仓无 `CACHES` 配置、无 `django-axes` / `django-ratelimit`（`requirements.txt` 仅 6 个包）。`/login/` 与 `/admin/login/` 暴露公网，可无限次尝试；审计日志里也没有登录失败记录（`accounts/views.py` 未记录 `auth.login.failure`），事后无法回溯爆破行为。

**放大因素**：`ForcePasswordChangeMiddleware`（`config/middleware.py:92-111`）只放行改密页与登出页，但**不阻止并发登录**。管理员发放的初始口令若遵循可猜模式（如 `学号后六位`、`abc123456`），在无锁定、无速率限制的情况下会被快速命中，且账号在改密前一直有效。

**修复**：
- Nginx `limit_req` 对 `/login/`、`/admin/login/` 单独设区（如 `10r/m burst=5 nodelay`）。
- 应用层引入 `django-axes`（失败计数 + 锁定时长 + 与审计联动），或在 `PlatformLoginView.form_invalid` 里记审计并做计数。
- 口令策略：`AUTH_PASSWORD_VALIDATORS` 保持现状（已含四件套），但对管理员发放的初始口令强制随机生成，避免可猜模式。

---

### V4（中）公网静态扫描文件

**现状**：`.github/CODEOWNERS` 会被 `git checkout` 到生产目录，Nginx 没有拒绝规则。其中记录了仓库与负责人信息；`.git/` 目录同样随代码落在 `APP_DIR`，且 `install.sh` 未对 `/.git/` 加拒绝规则（实测 Nginx 配置里只有 `/static/` 与 `/media/` 两个 location）。

**影响**：泄露组织内部信息；`.git/` 若可读会直接泄露全部源码历史（含历史上出现过的敏感串）。属于低成本高收益的加固项。

**修复**：在 `deploy/nginx-club702.conf` 的 server 块内加：

```nginx
location ~ /\.(?!well-known) { deny all; return 404; }
```

并确认 `APP_DIR/.git` 不可经 HTTP 读取（该规则已覆盖）。

---

### V5（中）上传原文件名落盘，评审匿名存在存储侧旁路

**现状**：
- 帖子图片用 uuid 重命名（`discussion/validators.py:36`）；
- 批注版与归档版落盘用 uuid（`reviews/models.py:67`）；
- **但头像、图册、媒体库、项目书保留原始文件名**（`accounts/models.py:79,149`、`media/models.py:17`、`projects/models.py:28` 都只是 `upload_to=".../%Y/%m/"`），实测 `get_valid_filename("项目书 终版-张三.docx")` → `项目书_终版-张三.docx`。
- 更关键的一处：`reviews/services.py:181-185` 归档时用 `os.path.basename(assignment.annotated_file.name)` 作为新文件名，而 `ArchivedProposal.file` 的 `upload_to=upload_archived_proposal` **会再次做 uuid 化**，所以归档路径本身仍是 uuid——这一点是对的。

**风险**：
1. 项目书文件名带组名/人名 → 评标场景下的信息泄露（可在评审期内暴露组员身份，虽不直接泄露评审人）。
2. 批注版原始文件名由评审人上传，若管理员在 `/admin/` 用媒体库字段替换批注版，或后续有人复用非 uuid 化的 `upload_to`，匿名性即从存储层破口。这是一条「当前成立、易被改坏」的边界，值得写进约束。

**修复**：
- 建议对**全部**上传统一 uuid 化落盘，把「用户填的名字」只存在数据库字段或只在 `Content-Disposition` 里体现；`core/uploads.py` 已集中了校验逻辑，适合再收一个 `neutral_upload_to` 工厂。
- 若要保留项目书原名（对用户更友好），至少在 `Content-Disposition` 与 `/media/` 直出之间做一次显式决定：当前 `/media/project_proposals/...` 是**无鉴权直出**的（见下）。

**附带发现（需确认）**：Nginx 对 `/media/` 是 `alias` 直出（`deploy/nginx-club702.conf:20-24`），而代码里对 4 类文件刻意走了带权限的 `FileResponse`。这意味着落在 `mediafiles/project_proposals/`、`mediafiles/uploads/`、`mediafiles/review_annotations/` 下的文件，**其真实路径一旦已知（UUID 不可猜，但项目书是「原名 + 年月」可猜），即可绕过权限检查直接下载**。请确认这符合设计意图——若 `mediafiles/` 目录只放「本就公开」的资源，应把项目书、批注版、归档版迁到非公开目录；若 `/media/` 需要保留，则应对这几类前缀单独加 Nginx 拒绝规则。

---

### V6（中）用户可控文本无长度上限

**现状**（实测枚举，以下字段的表单与模型都没有 `max_length`）：

| 字段 | 位置 | 可达角色 |
|---|---|---|
| 评审/初审意见 | `reviews/forms.py:61`、`reviews/models.py:262` | 评审人、初审人、超级评审 |
| 送审提交说明 | `reviews/forms.py:35`、`reviews/models.py:114` | 项目组联系人 |
| 入组申请理由 | `projects/forms.py:39` | 任意成员 |
| 建组申请描述 | `projects/forms.py:63`、`projects/models.py:199` | 任意成员 |
| 项目组介绍 | `projects/forms.py:71`、`projects/models.py:24` | 项目组联系人 |
| 竞赛报名备注 | `competitions/forms.py:30`、`competitions/models.py:81` | 项目组联系人 |

**对比**：帖子正文限 20000、评论限 4000、个人简介限 1000——说明作者有意识，只是这几处漏了。

**影响**：单次请求可写入数百 MB（受 `DATA_UPLOAD_MAX_MEMORY_SIZE` 2.5 MB 默认值约束，但该值未显式设置，随 Django 版本变化；且 `FILE_UPLOAD_MAX_MEMORY_SIZE` 之上会落临时文件）。可被用来快速膨胀数据库、拖慢后台列表与页面渲染；配合 V1 可放大可用性影响。

**修复**：为上述 6 处补 `max_length`（建议：意见/说明 5000，申请理由/组介绍 2000），并在 `settings.py` 显式写死 `DATA_UPLOAD_MAX_MEMORY_SIZE`、`FILE_UPLOAD_MAX_MEMORY_SIZE`，不要让它们依赖框架默认值。

---

### V7（中）备份与恢复的完整性问题

**现状**（`deploy/backup.sh`）：
- 备份产物写在 `APP_DIR/backups/`，与数据库、媒体、代码**同一台机器、同一个用户**。`club702.service` 的 `ReadWritePaths=@@APP_DIR@@` 让应用进程对整个目录有写权限——一旦应用被攻陷或主机被勒索，备份与数据一起消失。
- `pg_dump` 与 `tar` 是两次独立操作，数据库备份与媒体备份**不是同一时间点的快照**；恢复后可能出现「库里有记录、媒体文件缺失」或反之。
- 备份文件未加密。备份里包含实名身份、学号、手机号、全部评审意见——这是比在线库更集中的一份敏感数据。
- 脚本注释里已给出异地同步建议（`backup.sh:46-47`），但未纳入自动化。

**修复**：
1. 至少把 `backups/` 移出 `ReadWritePaths`，并同步到异地（对象存储 / 另一台主机），备份账号与运行账号分离。
2. 了解析度上，纯 `pg_dump` 一致性快照 + 媒体 rsync 增量可接受；若要严格一致，先 `SELECT pg_backup_start()` 或短暂停写再打包。
3. 为备份加密（如 `age` / `gpg`），并**至少做一次真实恢复演练**——未验证过的备份不算备份。

---

### V8（中低）依赖与构建链

- `requirements.txt` 全部是 `>=x,<y` 区间，**无锁文件**：同一 tag 在不同时间部署可能装上不同版本。对安全修补友好（能自动吃到补丁），但破坏「任意一版代码与当版数据库兼容」这一回滚前提（`docs/deploy.md:148` 明确依赖它）。
- 无 CI（`.github/` 下只有 `CODEOWNERS`）：翻译兜底测试、依赖漏洞扫描、`check --deploy` 都没有自动门禁。
- ~~`admin.E408`~~：**此处原文有误，已更正**。原文称中间件顺序会触发 `admin.E408` 并在 `check` 输出里留下永久噪音；实测 `manage.py check` 输出 `System check identified no issues (0 silenced)`，没有任何 admin 检查项。本报告不再把它列为待办。

**修复**：引入 `pip-compile`/`uv lock` 生成锁定文件；加一条最小 CI（`manage.py check --deploy` + 全部测试 + `pip-audit`）；对齐中间件顺序。

---

### V9（中低）评审保密与内容完整性的制度性缺口

1. **评审意见对全体组员可见**：`projects/permissions.py:61` 让 `can_view_group` 对组员放行，而项目组详情页会渲染每条评审任务的结论与意见（`templates/projects/group_detail.html:158`，`{{ assignment.comment }}`）。若组内有人同时是评审池成员，他可以从「我是组员」这一支读到别人对**本组以外**轮次的意见——注意此处每个组只能看到自己的轮次，所以**不构成跨组泄露**，但组内多人可见评审原文是否符合制度预期，需要确认。
2. **可回溯改写**：`discussion/permissions.py:34` `can_edit_post` 无时间窗，作者可随时改写已发布正文；评论已做软删除留痕（`discussion/models.py` 的 `CommentManager`），帖子却没有对应的历史版本。若论坛用于正式议题讨论，这会削弱内容作为证据的价值。
3. **审计日志不可删——已核实**：`core/admin.py:94-96` 的 `AuditLogAdmin.has_delete_permission` 明确 `return False`，是只读记录里唯一一条禁删的。此处无需改动，记录在此以说明「审计链条完整」这一结论有据。

**评级**：属于设计与制度层面，列出供决策，不是代码缺陷。

---

### V10（低中）密钥与配置管理

- `env.sh` 由 `install.sh` 从模板 `cp` 而来（`install.sh:34-35`），**脚本没有对它 `chmod 600`**，权限取决于 umask，可能是 644。同一主机上的其它低权账号可读走 DB 口令、`SECRET_KEY`、超管口令。
- `SECRET_KEY` 有可用默认值 `django-insecure-development-only-change-me`（`settings.py:22-25`）。生产用 `env.sh` 覆盖是正确做法，但没有任何检查阻止「忘记设置就上线」——`check --deploy` 的 W009 只会在有人主动跑时提示。
- `DEBUG` 默认 `True`（`settings.py:26`），依赖 `env.sh` 显式设 0。这与项目「安全默认」的其它部分（Cookie、NOSNIFF）不一致。

**修复**：
- `install.sh` 生成 `env.sh` 后加 `chmod 600 "$ENV_FILE"`。
- 生产环境下若 `SECRET_KEY` 仍为默认值或 `DEBUG=True`，直接在 `settings.py` 里 **抛异常拒绝启动**（比警告可靠）。注意这会与本地开发冲突，需用 `DJANGO_DEBUG` 判断。

---

### V11（低）竞赛报名放弃没有截止时间校验

**现状**：报名与**修改**都检查了 `Competition.is_registration_open`（`competitions/views.py:109,190`），但**放弃**没有（`competitions/views.py:230-251`），服务层 `withdraw_registration`（`competitions/services.py:82-98`）直接 `registration.delete()`。

**影响**：报名截止、甚至名单已经提交给竞赛主办方之后，项目组联系人仍可删除自己的报名记录。这不会造成越权或数据泄露，属于业务一致性缺口——真实后果是「已上报的名单与平台记录不一致」，由主办方与平台对账时才会发现。

**修复**：与报名/修改对齐，在视图或服务层加同一条 `is_registration_open` 判定（若业务上确实允许赛后撤回，则应显式记录在案并保留审计，而不是静默删除）。

---

## 五、建议的修复顺序

### 第 1 批 · 立刻（当天可完成）

| 项 | 动作 | 位置 |
|---|---|---|
| V1 | `except` 补 `DecompressionBombError`，并显式设 `Image.MAX_IMAGE_PIXELS` | `core/uploads.py` |
| V1 回归 | 为四条上传通道各加一条「声明超大尺寸的图片被拒绝且不抛异常」的测试 | 各 app `tests.py` |
| V4 | Nginx 拒绝 `/.` 开头的路径 | `deploy/nginx-club702.conf` |
| V10 | `install.sh` 生成 `env.sh` 后 `chmod 600` | `deploy/install.sh` |

### 第 2 批 · 一周内

| 项 | 动作 |
|---|---|
| V2 | 启用 HTTPS；80 整站跳 443；补 `SECURE_SSL_REDIRECT`、`SECURE_HSTS_SECONDS`；`deploy.sh` 加入 `check --deploy` 门禁 |
| V3 | Nginx `limit_req` 限 `/login/` 与 `/admin/login/`；登录失败记审计；初始口令强制随机 |
| V5 | 统一 uuid 化落盘（或明确 `/media/` 的公开边界并对项目书等前缀加拒绝规则） |
| V6 | 6 处文本补 `max_length`；显式设置 `DATA_UPLOAD_MAX_MEMORY_SIZE` |

### 第 3 批 · 一个月内

| 项 | 动作 |
|---|---|
| V7 | 备份出本地 + 加密 + 恢复演练 |
| V8 | 依赖锁定 + 最小 CI + 中间件顺序 |
| V9 | 就「组员可见评审意见」「帖子可回溯编辑」定制度 |

### 第 4 批 · 顺手加固（低危）

- `content/templatetags/rendering.py:41` 把 `code` 的 `class` 换成只放行 `language-*` 的可调用白名单。
- 若将来给 Markdown 链接加 `target="_blank"`，同步补 `rel="noopener noreferrer"`。
- 为 `competition_registration_withdraw` 补上截止时间判定（V11）。

---

## 六、长期要守住的约定

这份代码的安全性很大程度来自几条被认真执行的约定。它们没有写在测试里，改动时容易被无意破坏，建议固化成评审检查项：

1. **上传文件永不直出 `/media/`**——凡是有权限含义的文件，一律走视图判定后 `FileResponse`。新增上传模型时必须同时决定它属于哪一类。
2. **评审人的身份不进存储、不进文件名、不进页面**——落盘 uuid、下载名中性、页面只写序号。任何新增「评审相关文件」都要过这一关。
3. **权限判定只有一个出处**——「谁算管理员」问 `core.permissions.is_admin`，「谁能看这个组」问 `projects.permissions.can_view_group`。不要在视图里新写一套判断。
4. **状态变更走服务层 + 事务 + 审计**，并发正确性靠数据库约束（唯一约束、CheckConstraint、`select_for_update`），不靠应用层自觉。
5. **任何用户输入都经表单校验**，模型侧 `save()` 里 `full_clean()` 是最后一道，不是第一道。
6. **审计只记事实、不记敏感内容**（`core/audit.py:27`）。

---

## 附录 A：本次审查使用的验证命令

```bash
# 部署安全检查（生产参数）
source env.local.sh
DJANGO_DEBUG=0 DJANGO_ALLOWED_HOSTS=example.com .venv/bin/python manage.py check --deploy

# 注入面（应为零命中）
grep -rn "raw(\|extra(\|RawSQL\|cursor()\|\.execute(" --include=*.py . | grep -v '/.venv/'

# 危险调用（应为零命中）
grep -rn "eval(\|exec(\|subprocess\|os\.system\|pickle\|shell=True" --include=*.py . | grep -v '/.venv/'

# 模板安全面
grep -rn "|safe\|mark_safe\|autoescape" --include=*.py --include=*.html .
grep -rn "csrf_exempt" --include=*.py .

# CSRF 覆盖核对：每个模板 POST 表单数 vs {% csrf_token %} 数

# 中间件顺序与安全头
.venv/bin/python -c "from django.conf import settings; import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','config.settings'); django.setup(); print(settings.MIDDLEWARE)"
```

## 附录 B：解压炸弹复现脚本（用于回归测试）

```python
import io, struct, zlib

def png_with_declared_size(width, height):
    """只声明尺寸、不带像素数据的最小 PNG：99 字节左右，足以触发 Pillow 的解压炸弹检查。"""
    def chunk(tag, data):
        body = tag + data
        return struct.pack(">I", len(data)) + body + struct.pack(">I", zlib.crc32(body) & 0xFFFFFFFF)
    return (
        b"\x89PNG\r\n\x1a\n"
        + chunk(b"IHDR", struct.pack(">IIBBBBB", width, height, 8, 2, 0, 0, 0))
        + chunk(b"IDAT", zlib.compress(b"\x00" * width))
        + chunk(b"IEND", b"")
    )
# 期望：validate_image_upload 抛 django.core.exceptions.ValidationError，而不是 PIL.Image.DecompressionBombError
```

---

*审查完成于 2026-09-24，代码基线 `98564c5`。本文只覆盖静态代码审查与本地动态验证，未做渗透测试、未做生产环境配置核查、未做依赖 CVE 扫描；第 4 批之后的修复项建议在改动后重新跑一遍 §附录 A 的命令。*
