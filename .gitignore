# Python
__pycache__/
*.py[cod]
*.pyo

# Local environment
.venv/
.env

# Django runtime
staticfiles/
mediafiles/uploads/
mediafiles/project_proposals/
mediafiles/review_annotations/
mediafiles/review_archives/
mediafiles/avatars/
mediafiles/gallery/
# 受保护的上传件（项目书、批注版、头像、图册）落在这里，与 mediafiles/ 分开：
# 它不在 Nginx 的 /media/ 之下，只能经视图取。
protected_media/
# logs/*.log* 覆盖 RotatingFileHandler 的轮转备份（django.log.1、.log.gz 等）
logs/*.log*

# Editors
.vscode/
.idea/

# Claude Code 本地配置与 skill（run-local 里写着本地演示账号的密码，不进版本库）
.claude/

# 界面翻译的编译产物（.po 是源，部署与本地各自 compilemessages 生成）
*.mo

# Archive supplied separately
社团评审系统_发布版.zip

# 环境配置：env.sh 为服务器部署配置，env.local.sh 为本地开发配置
env.sh
env.local.sh
backups/
