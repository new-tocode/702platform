"""社团空间的验收测试，一个模块一个主题。

空间的四件事各自成模块——**访问与导航**（谁能进来）、**帖子**、**评论**、**板块**
管理；模型层、服务层、表单与只读查询另各一个。原先这六类挤在一个 1176 行的文件里，
其中 ``DiscussionViewTests`` 一个类就 504 行 17 个用例，混着帖子、评论、板块三件事。

共用件只有两处：:mod:`discussion.tests.factories`（两个临时媒体根与一张帖子配图）
与 :mod:`discussion.tests.base`（``DiscussionViewTestCase``：四个账号 + 一个板块）。
夹具这次**可以**共用——讨论区没有抽签，夹具不决定任何随机结果；``reviews/tests``
刻意不共用夹具是相反的情形（那里候选人数量会改变抽签结果）。
"""
