# 社团空间：评论可由发布者或管理员删除

现状：帖子能删，评论只能发——写错了没人能撤。这次把评论也纳入同一条口径：
**评论作者可删自己的评论，管理员可删任意评论**（与帖子的删除规则对称）。

- [x] 阶段 1：模型加 `deleted_at`／`deleted_by`，评论默认查询只返回未删除的
- [x] 阶段 2+3：权限、写操作、视图与路由（合并为一个提交；迁移随阶段 1 落地）
- [x] 阶段 4：界面（评论上的删除按钮与确认）与英文文案
- [x] 阶段 5：测试（服务层 + 视图层）
- [x] 阶段 6：同步文档，删掉本清单

判定与约束：
- 删除是**软删除**：评论行保留，内容保留，界面显示「该评论已删除」。硬删会让用户的
  评论凭空消失、事后无从追查；删除动作本身另有 `core.audit` 审计。
- 成员门槛、管理员口径沿用 `core.permissions.is_admin()`，与删帖一致。
- 模板仍只隐藏入口，真正的边界在视图门槛 + 服务层二次校验。
