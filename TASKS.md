# 社团空间实施清单

## 已确认决策

- 所有 `is_active=True` 且已完成首次改密的账号可进入社团空间。
- 置顶帖子排在普通帖子之前，各自区域按发布时间倒序。
- 板块创建同时填写中文名和英文名；两者都是用户内容、不进入翻译。帖子、评论和成员资料内容不自动翻译。
- 删除帖子时级联删除评论；有帖子时禁止删除板块。
- 只读成员资料展示头像、姓名/账号名、学院、专业、特长、简介、身份、个人图册、手机号和其他联系方式；不展示学号。
- 每一阶段通过对应检查后单独提交；不跨阶段合并提交。

## 阶段与验收

- [x] 1. 分支与任务清单：建立 `feature/club-space` 分支并维护本清单。（本阶段）
- [x] 2. 数据模型与迁移：新增独立 `discussion` app；建立 Board、Post、Comment 模型、约束、索引与迁移；验证空板块可删、有帖板块不可删，删除帖子会清除评论。
- [x] 3. 权限、服务与查询：按作者/管理员/超级管理员分层授权；所有写操作服务端校验；使用服务层与审计；板块删除与发帖并发安全；增加权限和服务测试。
- [x] 4. 路由与 SSR 视图：新增登录保护的空间、板块、帖子、评论及管理操作路由；编辑/删除/置顶/板块操作仅接受 POST；增加 HTTP 层测试。
- [x] 5. 成员目录与只读资料：活跃账号目录、姓名搜索、头像/姓名回退；增加只读个人资料页；测试资料字段白名单及不可写。
- [x] 6. 页面、导航与响应式：顶部“公开通知”右侧增加登录成员入口；新增帖子/评论/板块/成员 UI；更新导航高亮和移动导航；横向板块滑轨与独立成员滚动栏；加入前端板块创建/删除操作。
- [x] 7. 英文界面、文档与整体验收：补全英文 PO 和翻译测试；更新开发文档；通过检查、迁移一致性、测试及桌面/移动端浏览器验收。

## 权限验收矩阵

| 操作 | 游客 | 普通成员 | 平台管理员 | 超级管理员 |
|---|---:|---:|---:|---:|
| 查看空间/帖子/成员目录与只读资料 | 否 | 是 | 是 | 是 |
| 发帖、评论、编辑/删除自己的帖子 | 否 | 是 | 是 | 是 |
| 删除他人帖子、置顶/取消置顶 | 否 | 否 | 是 | 是 |
| 创建/删除板块（板块必须为空） | 否 | 否 | 否 | 是 |

## 后续增强清单

- [x] 8. 双语板块名与滑轨：增加必填中文名，保留必填英文名；板块条左侧标注“板块”，放大标签并并列呈现中英文名称；历史板块以原英文名回填中文名并补充测试。
- [ ] 9. 帖子图片数据与服务：新增帖子图片模型，复用真实图片校验；每帖最多 3 张、每张 ≤3 MiB；编辑可移除/补加图片，删帖或移除图片时清理文件，并覆盖并发上限与文件清理测试。
- [ ] 10. 图片交互、翻译与收尾：帖子表单支持多图上传与现有图预览/移除，评论区明显缩进；更新英文翻译和架构/开发文档；全量回归后单独提交。

## 最终验证

- `source env.local.sh`
- `.venv/bin/python -m compileall -q config accounts discussion core`
- `.venv/bin/python manage.py check`
- `.venv/bin/python manage.py makemigrations --check --dry-run`
- `.venv/bin/python manage.py test`
- 更新并编译 `locale/en/LC_MESSAGES/django.po`，运行 `core.tests.InterfaceTranslationAcceptanceTests`
- 用 `run-local` 在桌面与窄屏验证导航、板块滑动、成员栏、帖子图片、帖子权限和英文页面。
